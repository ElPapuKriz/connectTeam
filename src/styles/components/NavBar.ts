import type { CSSProperties } from "react";

export const styleNavBar:CSSProperties = {
    backgroundColor:'red',
    display:'flex',
    flexDirection:'row',
    justifyContent:'space-between',
    padding:'20px'
}

